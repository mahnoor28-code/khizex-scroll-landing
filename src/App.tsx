import React from "react";

/**
 * Phase 1 placeholder. This will be replaced section-by-section starting
 * Phase 2 (page layout) — for now it just proves the build pipeline works.
 */
export default function App(): React.ReactElement {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center" }}>
      <h1>KHIZEX scroll landing — Phase 1 skeleton is running ✅</h1>
    </main>
  );
}
